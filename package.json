{
  "name": "insight-flow",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --dir packages/taskflow run build",
    "build:package": "pnpm --dir packages/taskflow run build",
    "pack:taskflow": "pnpm --dir packages/taskflow exec npm pack",
    "yalc:publish": "pnpm --dir packages/taskflow run build && pnpm --dir packages/taskflow exec yalc publish",
    "yalc:push": "pnpm --dir packages/taskflow run build && pnpm --dir packages/taskflow exec yalc push",
    "play": "pnpm --dir playground run ui",
    "ui": "node packages/taskflow/dist/cli.js ui",
    "master": "node packages/taskflow/dist/cli.js master",
    "master:dev": "pnpm --dir packages/taskflow run build && node packages/taskflow/dist/cli.js master",
    "prepare": "husky"
  },
  "lint-staged": {
    "packages/taskflow/src/**/*.ts": [
      "pnpm --dir packages/taskflow exec prettier --write",
      "pnpm --dir packages/taskflow exec eslint --fix"
    ]
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.7",
    "yalc": "1.0.0-pre.53"
  }
}
