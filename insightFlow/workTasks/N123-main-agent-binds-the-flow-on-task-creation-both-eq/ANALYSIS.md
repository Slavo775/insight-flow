# ANALYSIS — N123

Part of the program **"flows govern everything" (N119–N133, 5 epics)**. The full strategist analysis
(problem framing, the three settled forks, options, decisions, open questions, sources) lives in
[`N119-*/ANALYSIS.md`](../N119-eject-override-loader-insightflow-shadows-shipped-/ANALYSIS.md).

Binding decisions: editable defaults via eject/override with three tiers (custom CRUD / default
eject-to-edit / locked read-only); flow selection "both equal" (main-agent invoke OR type→flow map);
FULL custom statuses reached via transitions-as-data (a status-transition module kind), staged so the
canonical default flow stays byte-identical until the final E5 flip. Default-flow parity is the safety
contract on every status/picker/prompt task.
