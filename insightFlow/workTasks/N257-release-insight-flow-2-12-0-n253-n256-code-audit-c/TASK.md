# N257 — Release insight-flow 2.12.0 — N253–N256 code-audit cleanup

**Type:** chore
**Priority:** high
**Created:** 2026-07-21

## Problem

Tasks N253–N256 (code-audit cleanup) are merged to `main` (squash `6239581`) and green, but no release has been cut. The release check (N257, `release-checked`) found the work is release-worthy as a **minor (2.12.0)** — but the merge landed as a `refactor(taskflow)` commit, and this repo auto-releases via **release-please**, which only bumps on `feat:` (minor) / `fix:` (patch). A `refactor:`/`chore:` batch produces **no release PR**, so 2.12.0 will not happen automatically.

## Goal

1. Trigger release-please to open the 2.12.0 (or agreed) release PR for the N253–N256 work.
2. Ensure the one user-facing behavior change (256KB master-POST body cap → 413) is surfaced in the CHANGELOG (a `refactor:` commit is hidden under release-please defaults).
3. Land the release: version bump + tag + npm publish (publish env needs manual approval — see release-publish workflow).

## Scope

### In scope

- The release-trigger decision + its commit (see Open decision in Notes).
- `packages/taskflow/CHANGELOG.md` — a note for the 256KB body-cap / 413 change (only if not auto-generated by the chosen trigger).
- Optional: `website/docs/built-ins/master-server.md` — one line on the POST body limit.

### Out of scope

- Any source/behavior change — N253–N256 are already merged, reviewed, and done. This task only releases them.
- Re-opening the code review.

## Implementation plan

1. **Resolve the release-please trigger** (human decision — see Notes "Open decision"): add a `feat:`-typed empty/release commit for a 2.12.0 minor, OR relabel the change as `fix:` for a 2.11.1 patch, OR manual bump+tag+changelog.
2. **Changelog note** — ensure the 256KB body-cap / 413 behavior change appears in the release notes (manual line if the trigger commit's type would hide it).
3. **(optional) Doc note** — 256KB POST limit in `master-server.md`.
4. **Merge the release-please PR** — bumps `package.json` + `.release-please-manifest.json`, updates CHANGELOG, tags.
5. **Publish** — the `release-publish` workflow publishes to npm; its env needs manual approval (`gh api ... pending_deployments`). Pin `npm@^11.5.1` (npm@latest v12 breaks Node 20 publish).
6. **Verify** — `npm view insight-flow version` shows the new version; tag pushed.

## Verification

- Tests green (374/374 — already confirmed by the checker).
- release-please PR opened and merged; new tag `v2.12.0` (or agreed version).
- `npm view insight-flow version` == released version.
- CHANGELOG shows the N253–N256 summary + the body-cap note.

## Notes

**Checker findings (N257 `release-checked`):** tests 374/374 pass; build + full typecheck clean; intent = feature/minor → **2.12.0** (additive exports `sendJson`/`readBody`/`buildEnforcementBlock` etc., zero removals, `SseTransport` names preserved); docs essentially up to date (removed endpoints were never documented).

**Open decision (blocks release — needs human):** the merged commit is `refactor:`, which release-please ignores. Pick one:
- **(A) Force 2.12.0 minor** — add a `feat:`-typed commit/note so release-please cuts a minor. Matches the additive-API intent.
- **(B) Patch 2.11.1** — treat the 256KB cap as a `fix:` ("closes an unbounded-body hole"); release-please cuts a patch. Undersells the new exports.
- **(C) Manual** — bump `package.json` + manifest, hand-write CHANGELOG, tag `v2.12.0`, let the publish workflow run.

Related: [N253], [N254], [N255], [N256]. Last release (2.11.0) triggered via `feat(master)` #166.
