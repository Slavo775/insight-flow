{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "outDir": "dist",
    "rootDir": "src",
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  },
  "include": ["src"],
  "comment": "The React dashboard client (src/dashboard/client) is built by Vite and type-checked via its own tsconfig (typecheck:client) — excluded here so the CLI's tsc doesn't choke on JSX/DOM.",
  "exclude": ["src/dashboard/client", "node_modules", "dist"]
}
