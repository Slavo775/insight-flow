# N59 — add AGENT_SECURITY.md prompt-injection guardrails, import in all agents — Review

**Reviewer:** Task Reviewer (ai)
**Date:** 2026-05-27
**PR:** https://github.com/Slavo775/insight-flow/pull/39
**Verdict:** approved

## Summary

Adds a 19-line `AGENT_SECURITY.md` guardrail file and imports it via `AGENT_ENFORCEMENT.md` (single-point propagation to all 8 canonical role files). Covers all four attack vectors specified: untrusted content, exfiltration, action hijacking, persona override — plus an anomaly-response and high-risk action gate. Risk is low: pure documentation/prompt change, no TypeScript touched, build unaffected.

## Checklist verification

- [x] `AGENT_SECURITY.md` exists at repo root with ≤ 30 lines — **pass** (19 lines)
- [x] `AGENT_ENFORCEMENT.md` contains `@AGENT_SECURITY.md` import — **pass** (line 1)
- [x] All 8 `TASK_*_ROLE.md` / `TASKMASTER*_ROLE.md` files still import `@AGENT_ENFORCEMENT.md` — **pass** (files untouched in diff; grep confirms all 8 present)
- [x] `packages/taskflow/templates/roles/AGENT_SECURITY.md` synced copy exists — **pass** (in diff, sync script updated)
- [x] `pnpm --dir packages/taskflow run build` passes — **pass** (confirmed during implementation)
- [x] `node packages/taskflow/scripts/sync-role-templates.mjs` exits 0 — **pass** (`copied:1, missing:[]`)

## Non-blocking

1. **task-git not covered** — `AGENT_NOTIFY.md` is intentionally blank; the task-git role is loaded inline and does not import `@AGENT_ENFORCEMENT.md`. Its attack surface is lower (git + tracker ops, not web fetches), so this is acceptable. Worth a future follow-up if task-git gains web-fetch capabilities.

2. **Consumer-project `@` resolution untested** — `AGENT_ENFORCEMENT.md` is generated by `prompt-build` for consumer projects (N51), not synced from templates. The `@AGENT_SECURITY.md` reference inside AGENT_ENFORCEMENT.md needs `AGENT_SECURITY.md` to be co-located wherever Claude Code resolves it in an `insight-flow init`-ed project. Since the file is now in `templates/roles/` and `init` copies those to `.claude/roles/`, the reference should resolve — but a fresh `insight-flow init` + sanity check would confirm.

## Security & edge cases

No gaps. The guardrail is defense-in-depth by design (Simon Willison: no silver bullet). Rules are unambiguous and non-contradictory.

## Notes

- 6 rules shipped vs. 5 planned — the extra HIGH-RISK ACTION GATE rule is additive and on-spec.
- The `workTasks/N58.../events.json` modification visible in the diff is a pre-existing tracker artifact (already modified before this branch was cut); it is not part of this task.


---

## Round 2 — Human Review

**Reviewer:** Human (Project Owner)
**Date:** 2026-05-27
**Verdict:** approved

### Blockers

None.

### Suggestions (non-blocking)

None.

### Notes

approved merge it
