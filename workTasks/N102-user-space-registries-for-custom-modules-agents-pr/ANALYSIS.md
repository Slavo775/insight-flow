# ANALYSIS — N102

This task is part of the round **N99–N112: insightFlow layout + customization layer + prescriptive-lite flow**.

The full strategist analysis (problem framing, options considered, decisions, open questions, sources) lives in
[`workTasks/N99-insightflow-layout-path-indirection-back-compat-sh/ANALYSIS.md`](../N99-insightflow-layout-path-indirection-back-compat-sh/ANALYSIS.md).

Key decisions binding this task: full layout migration with back-compat shim; custom artifacts are user-space
(`custom:` namespaced) with built-ins immutable; flows power suggestions only (pickers/state machine canonical);
custom states are alias-based and land last (N112).
